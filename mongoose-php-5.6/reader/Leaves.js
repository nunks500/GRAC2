
  function Leaves(id,type,args) {
    this.id = id;
    this.args = args;
    this.type = type;
    
  
  }



  Leaves.prototype.getDesc = function() {
   // return this.name + ' ' + this.colour + ' ' + country;
  };
